class Question:
    def __init__(self, q_text, q_answer):
        # must use this syntax self.nameOfAttribute -> when create new object, will pass in these 2
        self.text = q_text
        self.answer = q_answer
